
@{
    ViewBag.Title = "AddUserPreference";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<div class="relative">
    <nav>
        <!-- breadcrumb -->
        <ol class="flex flex-wrap pt-1 mr-12 bg-transparent rounded-lg sm:mr-16">
            <li class="text-sm leading-normal">
                <a class="opacity-50 text-slate-700" href="javascript:;">Preferences</a>
            </li>
            <li class="text-sm pl-2 capitalize leading-normal text-slate-700 before:float-left before:pr-2 before:text-gray-600 before:content-['/']" aria-current="page">
                Manage Preferences
            </li>
        </ol>
    </nav>
    <br />

</div>


<div class="flex flex-wrap -mx-3">
    <div class="flex-none w-full max-w-full px-3">
        <div class="relative flex flex-col min-w-0 mb-6 break-words bg-white border-0 border-transparent border-solid shadow-soft-xl rounded-2xl bg-clip-border">
            <div class="p-6 pb-0 mb-0 bg-white border-b-0 border-b-solid rounded-t-2xl border-b-transparent">
                My Preferences
            </div>
            @Html.Partial("~/Areas/Admin/Views/Shared/MessageForm.cshtml")
            <div class="flex-auto px-0 pt-0 pb-2">

                <div class="p-0 overflow-x-auto">
                    @using (Html.BeginForm("AddUserPreference", "UserPreference", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.Partial("Partial/PreferenceForm")
                    }
                </div>
            </div>
        </div>
    </div>
</div>

